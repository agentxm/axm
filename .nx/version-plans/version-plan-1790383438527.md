---
__default__: minor
---

Device-code sign-in no longer prints security warnings or copies to the clipboard on its own; pressing `c` copies the pre-filled link, over SSH through the terminal (OSC 52). `axm login` also chooses device code on Linux outside WSL when no display or `BROWSER` is set.
