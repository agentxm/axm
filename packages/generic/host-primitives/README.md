# Host primitives

This package owns atomic single-file replacement, SHA-512 SRI digests, file locations, process-environment readers, and user-home resolution for AXM. It imports no other AXM package so every tier can consume the same host behavior. The API is unstable and unsupported; use the axm.sh CLI.
