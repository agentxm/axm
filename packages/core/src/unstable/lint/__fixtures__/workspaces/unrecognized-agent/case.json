{
  "description": "settings.agents[] includes an agent id not in AXM's known-agent catalog. Surfaces as workspace/settings-schema-valid because the schema already validates agent ids; no workspace/agents-recognized finding (the schema arm catches it first).",
  "state": {
    "settings": { "agents": ["not-a-real-agent"] },
    "existingPaths": [".axm", ".axm/settings.json"],
    "listings": {},
    "detectedProjectAgents": []
  },
  "scope": "project",
  "expectedFindings": [
    {
      "ruleId": "workspace/settings-schema-valid",
      "severity": "error",
      "file": ".axm/settings.json"
    }
  ]
}
