# Specs

No specification changes required.

This change is implementation consolidation only. The existing `schema-settings` spec already defines the correct behavior (all fields optional). The implementation in `skills/settings.ts` diverged from the spec and needs to be brought back into alignment.

See: `openspec/specs/schema-settings/spec.md`
