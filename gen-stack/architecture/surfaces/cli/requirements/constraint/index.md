# Constraint Requirements

Binding limits on the AXM CLI's permitted override behavior.

- [Force bypasses only forceable policies](force-bypasses-only-forceable-policies.md) -
  On every AXM command, `--force` bypasses only an explicitly named forceable
  policy and never a hard invariant.
