{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "core",
  "projectType": "library",
  "root": "packages/core",
  "sourceRoot": "packages/core/src",
  "tags": ["type:lib", "scope:core"],
  "targets": {
    "generate-schemas": {
      "cache": true,
      "inputs": [
        "production",
        "^production",
        "{workspaceRoot}/packages/cli/scripts/generate-schemas.ts",
        "{workspaceRoot}/.prettierrc.json"
      ],
      "outputs": ["{projectRoot}/site-content/__generated__/schemas"],
      "executor": "nx:run-commands",
      "options": {
        "command": ["bun", "../cli/scripts/generate-schemas.ts"],
        "cwd": "packages/core"
      }
    },
    "gen-error-catalog": {
      "cache": true,
      "inputs": [
        "production",
        "{workspaceRoot}/packages/*/src/**/*.test.ts",
        "{workspaceRoot}/scripts/generate-error-catalog.ts"
      ],
      "outputs": [
        "{workspaceRoot}/docs/error-codes.md",
        "{projectRoot}/src/unstable/app-error/__generated__/catalog.ts"
      ],
      "executor": "nx:run-commands",
      "options": {
        "command": "bun scripts/generate-error-catalog.ts",
        "cwd": "{workspaceRoot}"
      }
    },
    "build": {
      "cache": true,
      "dependsOn": ["generate-schemas", "^build"],
      "executor": "@nx/js:tsc",
      "inputs": ["production", "^production"],
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "main": "packages/core/src/unstable/app-error/index.ts",
        "outputPath": "packages/core/dist",
        "tsConfig": "packages/core/tsconfig.lib.json",
        "clean": true,
        "generatePackageJson": false
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["{projectRoot}/**/*.{ts,tsx,js,mjs,cjs}"]
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "command": "pnpm exec vitest run",
        "cwd": "packages/core"
      }
    },
    "nx-release-publish": {
      "cache": false,
      "dependsOn": ["build", "^nx-release-publish"],
      "executor": "@nx/js:release-publish"
    }
  }
}
