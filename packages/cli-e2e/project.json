{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "cli-e2e",
  "projectType": "application",
  "root": "packages/cli-e2e",
  "sourceRoot": "packages/cli-e2e/src",
  "tags": ["type:e2e", "scope:cli"],
  "targets": {
    "e2e-main": {
      "dependsOn": [
        {
          "projects": "core",
          "target": "check-build-outputs"
        },
        "cli:build"
      ],
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec vitest run",
        "cwd": "packages/cli-e2e"
      }
    },
    "e2e": {
      "cache": false,
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          {
            "command": "pnpm exec nx run cli-e2e:e2e-main",
            "forwardAllArgs": true
          },
          {
            "command": "pnpm exec nx run cli-e2e:binary-smoke",
            "forwardAllArgs": true
          },
          {
            "command": "pnpm exec nx run cli-e2e:install-verification",
            "forwardAllArgs": true
          }
        ]
      }
    },
    "binary-smoke": {
      "dependsOn": [
        {
          "projects": "core",
          "target": "check-build-outputs"
        },
        "cli:compile"
      ],
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec vitest run --config vitest.binary.config.ts",
        "cwd": "packages/cli-e2e"
      }
    },
    "binary-smoke-ci": {
      "dependsOn": [
        {
          "projects": "core",
          "target": "check-build-outputs"
        },
        "cli:build"
      ],
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec vitest run --config vitest.binary.config.ts",
        "cwd": "packages/cli-e2e"
      }
    },
    "install-verification": {
      "dependsOn": [
        {
          "projects": "core",
          "target": "check-build-outputs"
        },
        "cli:compile"
      ],
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec vitest run --config vitest.install.config.ts",
        "cwd": "packages/cli-e2e"
      }
    }
  }
}
