/**
 * Extension resolution — REMOVED.
 *
 * resolveExtension has been removed. Resolution now only produces Source
 * via resolveSource(). Extension discovery is exclusively through
 * SourceHostProviders.find().
 *
 * @experimental This API is unstable and may change without notice.
 * @packageDocumentation
 */
