---
__default__: minor
---

Open rule publishing end to end (AXM-985 W6 PR11): rule joins PUBLISHABLE_TYPES, the publish lint gate evaluates the rule manifest catalog, axm rules publish is registered, and the e2e matrices drive the full scaffold-publish-install round trip for rules over both file and HTTP registries. Verified against the deployed registry, which routes the rule type and returns the current problem-details contract. The parity exemption ledger is now empty: every catalog extension type meets every mechanically verified obligation.
