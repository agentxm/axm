{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "extension-model",
  "projectType": "library",
  "root": "packages/extension-model",
  "sourceRoot": "packages/extension-model/src",
  "tags": ["type:lib", "scope:extension-model"],
  "targets": {
    "build": {
      "cache": true,
      "dependsOn": ["^build"],
      "executor": "@nx/js:tsc",
      "inputs": ["production", "^production"],
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "main": "packages/extension-model/src/unstable/extensions/index.ts",
        "outputPath": "packages/extension-model/dist",
        "tsConfig": "packages/extension-model/tsconfig.lib.json",
        "clean": true,
        "generatePackageJson": false
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["{projectRoot}/**/*.{ts,tsx,js,mjs,cjs}"]
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "command": "pnpm exec vitest run",
        "cwd": "packages/extension-model"
      }
    },
    "nx-release-publish": {
      "cache": false,
      "dependsOn": ["build", "^nx-release-publish"],
      "executor": "@nx/js:release-publish"
    }
  }
}
