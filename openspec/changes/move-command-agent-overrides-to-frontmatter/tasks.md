## 1. Implementation

- [x] 1.1 Remove command manifest `agentOverrides` schema support.
- [x] 1.2 Parse `agentOverrides` from command content frontmatter.
- [x] 1.3 Source render overrides from parsed content and strip the meta key before rendering.
- [x] 1.4 Reject old `command.json` `agentOverrides` during publish validation.
- [x] 1.5 Migrate in-tree examples and docs.
- [x] 1.6 Add regression tests for parsing, render plumbing, lossy renderers, and publish validation.
