# Change: Add CLI Extensions Sub-command

## Why

The CLI needs a top-level `extensions` sub-command to house extension management capabilities. This stub provides the foundation for future sub-commands.

## What Changes

- Add `axm extensions` sub-command with placeholder behavior
- Establish extension command namespace for future sub-commands

## Impact

- Affected specs: cli
- Affected code: `packages/cli/`
