# Specs: audit-codebase-conformance

This change is a pure refactoring effort to align existing code with CLAUDE.md conventions.

**No user-facing behavior is added, modified, or removed.**

The codebase will have the same external behavior after these changes. Only internal implementation details change to match current Effect and TypeScript conventions documented in CLAUDE.md.

See `proposal.md` for the full list of internal changes.
