[tools]
node = "22.23.2"
# pnpm's standalone artifacts do not include Intel macOS; the npm backend is
# cross-platform while keeping mise as the workspace tool authority.
"npm:pnpm" = "11.20.0"
bun = "1.3.14"
