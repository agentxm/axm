export NX_TUI=false
pnpm sync

if ! git diff --quiet --exit-code; then
  echo "nx sync updated workspace files. Review and commit those changes before pushing."
  exit 1
fi

pnpm nx affected -t e2e --parallel=1
